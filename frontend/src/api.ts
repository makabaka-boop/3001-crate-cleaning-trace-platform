export const api=async(path:string, options?:RequestInit)=>{const r=await fetch(`/api${path}`,{...options,headers:{'Content-Type':'application/json',...options?.headers}});if(!r.ok){const x=await r.json().catch(()=>({}));const d=Array.isArray(x.detail)?x.detail.map((i:{msg?:string})=>i.msg||JSON.stringify(i)).join('；'):x.detail;const e=new Error(d||`请求失败 (${r.status})`)as Error&{status?:number};e.status=r.status;throw e}return r.json()};
